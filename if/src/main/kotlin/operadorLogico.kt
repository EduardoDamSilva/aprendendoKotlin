fun main(){
    //Comparação de valores
    // >, <, <=, >=
    //Comparação de igualdade
    // ==, !=, !
    // Relação entre valore
    // &&, ||,

    // Avaliar intervalo de valores
    val n = 2
    if(n >= 1 && n <= 50) {

    }

    // OU

    if (n in 1..50){

    }
}